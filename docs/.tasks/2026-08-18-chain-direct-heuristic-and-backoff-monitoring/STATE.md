# Task: chain-direct-heuristic-and-backoff-monitoring

Mode: Attended
Phase: DOC-POST-IMPL
Handoff to: task-orchestrator (Publish)
Status: Green phase complete. software-engineer committed Task 2 Step 8 (commands/cairn-run-task.md, README.md, CLAUDE.md, tests/smoke/test_cairn_run_task_backoff_handoff_safety.sh — commit 8ca0a5a) and Task 3's version bump (0.17.0 -> 0.18.0, read from main not the worktree, per the plan's own instruction — commit 97c3ee9). Full `pytest tests/ -v -s` (Task 3 Step 2's final validation, including the eval suite the earlier partial check hadn't reached) confirmed: 30 passed — tests/test_usage_dashboard.py 29/29 green, tests/test_intent_routing.py::test_routing_accuracy PASSED at 16/17 (aggregate, above MIN_PASS; the one miss was a 120s tool timeout on the "review the README" case, not a misclassification). `claude plugin validate . --strict` also reconfirmed clean post-bump. All three plan tasks (heuristic, backoff loop, version bump) implemented and committed in full — not partial like the pr-reviewer sibling task's Task-1-only scoping. Handing off to qa-auditor.
Plan: docs/.plans/2026-08-18-chain-direct-heuristic-and-backoff-monitoring.md
Ticket: none
Worktree: /Users/jaysondelosreyes/cairn/.worktrees/feature-chain-direct-heuristic-and-backoff-monitoring
Branch: feature/chain-direct-heuristic-and-backoff-monitoring
Key info: TEST FIX applied to tests/smoke/test_cairn_run_task_backoff_handoff_safety.sh:152 per software-engineer's diagnosis. Old regex: `grep -qiE 'which (one|task)|please (specify|provide)|no target given'` — the `no target given` alternative false-positived on correct agent narration containing that literal phrase while describing correctly-detected backoff loop mode (e.g. "Backoff loop pass, no target given. Found 2 Unattended task, no STALLED marker on either..."). Fix: dropped the `no target given` alternative entirely (chosen over tightening it, since `which (one|task)|please (specify|provide)` already fully cover the real disambiguation-fallback signal and no false positive involved either of those two patterns). New regex: `grep -qiE 'which (one|task)|please (specify|provide)'`. Verification: (1) 5 consecutive full 3-tick runs against the current (uncommitted) backoff-loop-mode implementation — 0/5 failures, 0 false positives across 15 total tick checks. (2) Control: `git stash`'d the uncommitted commands/cairn-run-task.md + README.md changes (reverting to pre-implementation state), reran the test once — failed all 3 ticks on the tightened regex exactly as expected, output showing genuine disambiguation narration ("No target given. Two task folder found... Which one run/resume?") correctly matched by `which (one|task)`; confirms the test still fires correctly on real disambiguation-fallback behavior, not just weakened into a no-op. `git stash pop` restored the implementation afterward — commands/cairn-run-task.md/README.md untouched by this fix, only the test file was edited. No other assertions/lines in the test file were touched. Prior investigation of the reported tmux-session-kill-on-tick-1 issue (documented by software-engineer, 11 additional runs, zero reproductions, defense-in-depth guardrail sentence added) stands unchanged — not revisited here. claude plugin validate . --strict passes (unaffected by a test-only change).
Harness flags: none
