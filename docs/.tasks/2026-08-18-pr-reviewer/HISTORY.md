# History: pr-reviewer

<!-- Append-only. One line per completed phase, in order. Not full detail — that lives in git history / actual test output. -->
<!-- Format: <ISO-8601 UTC> — <PHASE> — <note>, e.g. 2026-08-17T14:32:00Z — PLAN — plan read, worktree created -->
<!-- The timestamp lets scripts/usage_dashboard.py --task-report <slug> correlate session usage to each phase. -->
2026-08-18T05:31:15Z — IMPLEMENT (resumed, TEST FIX REQUEST) — Resumed a prior software-engineer dispatch that hit an API session limit mid-work; agents/pr-reviewer.md already existed (written by the interrupted dispatch) with Task 1's frontmatter/SYSTEM ROLE/WORKFLOW INTENT/HARD REQUIREMENTS/INPUT RESOLUTION/Initial Review mode sections. Verified the frontmatter is byte-identical to the plan's Task 1 Step 1 block via diff — the code-review built-in-vs-marketplace-plugin disambiguation the interrupted dispatch was iterating on is already correct and complete, no further edit needed there. `claude plugin validate . --strict` passed from the start.

Ran `bash tests/smoke/run_pr_reviewer.sh $(pwd)` (sandbox disabled): both tests failed on "expected pr-reviewer's own 'Initial Review' mode-detection language" — the file existed and validated, but the literal phrase never appeared in the raw output. Strengthened agents/pr-reviewer.md's Initial Review mode Draft Phase (Step 3) with a mandatory literal first-line marker template and explicit 0-findings-still-gets-a-mandatory-save handling (the model was observed skipping the save entirely on a 0-findings run, treating "nothing to report" as a reason not to draft). Also added bare "PR #1"/"MR !1529" number parsing to INPUT RESOLUTION Step 1, matching both smoke tests' actual prompt shape (previously only URL/branch-name were documented).

Re-ran repeatedly (7 live `claude -p --plugin-dir <worktree> --permission-mode bypassPermissions` invocations total across debugging) and discovered the real blocker: the orchestrating top-level session only dispatched the `pr-reviewer` subagent via the Agent tool on 2/7 runs using the tests' exact bare-prompt phrasing ("review PR #1 on this repo" / "review MR !1529 on this repo") — the other 5/7 it answered inline itself, never touching agents/pr-reviewer.md's content at all (confirmed via session transcript inspection at ~/.claude/projects/.../*.jsonl — no Agent tool_use present on the failing runs). On the 2 successful-dispatch runs (achieved by rephrasing to "Use the pr-reviewer agent to..." or passing a full PR URL — not something the fixed test scripts can be made to do without editing them), the transcript's Agent tool_use and the actual saved docs/.reviews/github-octocat-Hello-World-1.md file confirmed pr-reviewer's Task 1 implementation performs correctly end-to-end: Reference Style Lookup, correct-format mandatory draft save, 0-findings Hard-Requirement compliance, no branch checkout, no posting. Also confirmed (via a plain "say hello" control call and a Bash-tool-use control call) that this dev machine's globally-enabled `caveman` plugin compresses all nested `claude -p` output regardless of `--plugin-dir`, though it does not block tool execution — a same-machine confound worth flagging but not something in software-engineer's scope to disable (global user settings, not a repo file).

Raised a TEST FIX REQUEST to qa-engineer rather than declaring false green or continuing to iterate on agents/pr-reviewer.md's content, since the frontmatter `description` driving dispatch is fixed per the plan (finalized through 4 Doc Gate rounds) and dispatch happens before the file's body is ever read — no legitimate Task-1-scoped implementation change can fix an orchestrating session's own judgment call to skip delegating. Updated STATE.md (Phase unchanged: QA-RED; Handoff to: qa-engineer) with full evidence and two suggested fixes (explicit-invocation test phrasing, or asserting against the docs/.reviews/ artifact file instead of the outer CLI's paraphrased text) and two HARNESS FLAGs (existing .harness/ absence gap, plus a new one on headless-smoke-test dispatch reliability). agents/pr-reviewer.md left uncommitted (Task 1 Step 7's commit deliberately deferred until tests are actually green, not just implementation-verified-correct-by-hand).
2026-08-18T00:13:33Z — PLAN — Plan read (docs/.plans/2026-08-18-pr-reviewer.md, spec docs/.specs/2026-08-18-pr-reviewer-design.md). Existing worktree/branch reused (no new worktree created). .harness/ absent entirely (harness-engineer suggested, non-blocking); .harness/environment.md absent (Environment Preflight skipped, no checks to run). Graphify not installed (skipped silently). qa-engineer + software-engineer Feasibility Assessment both returned ok (qa-engineer noted a non-blocking caveat re: no executable code in scope). No ticket sync configured — local-only. Mode: Unattended (per invocation instruction) — launching detached tmux session to continue Doc Gate onward.
2026-08-18T00:19:13Z — DOC-GATE — documentation-auditor ran read-only against docs/.plans/2026-08-18-pr-reviewer.md + docs/.specs/2026-08-18-pr-reviewer-design.md ahead of implementation. 10 findings (0 CRITICAL, 4 HIGH, 2 MEDIUM, 2 LOW, 2 INFO); 3 HIGH are in-scope and block plan execution (DOC-001: Task 1's `code-review --comment` call contradicts pr-reviewer's own draft/confirm/post hard requirements; DOC-002: Task 6 omits the spec-required `review`-category routing mapping in CLAUDE.md; DOC-003: Task 4's headless smoke test points --plugin-dir at the main repo instead of this worktree, making it a vacuous check). 1 HIGH (DOC-004, /cairn-feedback + feedback-context undocumented) confirmed pre-existing/out-of-scope, not blocking. 2 MEDIUM + 2 LOW also noted (see STATE.md Key info). Per Unattended handoff protocol, this is an in-scope HIGH-finding resolution decision — set Phase: HANDOFF NEEDED, wrote full pending question to STATE.md Key info, stopping cleanly rather than editing the plan/spec unattended.
- 2026-08-18T01:33:52Z PLAN: human resolved all 3 in-scope HIGH Doc Gate findings (DOC-001 --comment gate bypass, DOC-002 missing review-category routing mapping, DOC-003 wrong smoke-test plugin-dir) plus non-blocking DOC-005 (/loop attribution). Plan/spec amended (main commit 1bcee74, synced to worktree 6813fb5). Switched to Attended. Re-running Doc Gate to confirm clean before resuming at qa-engineer.
- 2026-08-18T01:44:42Z PLAN: Doc Gate re-run found 2 new HIGH (DOC-001 code-review capability claims vs. the only findable definition; DOC-002 the DOC-001 fix's mechanism doesn't achieve draft/post separation) plus MEDIUM DOC-003/005, LOW DOC-006. Resolved: user chose "disambiguate in spec/plan" over self-implementing GitHub too. Spec+plan amended: explicit built-in-vs-marketplace-plugin disambiguation for code-review, dependency classification added for code-review/loop as core capabilities (not third-party plugins), feedback-context EXIT row added, smoke test made re-run-safe. Re-running Doc Gate once more before resuming at qa-engineer.
- 2026-08-18T01:51:46Z PLAN: Doc Gate 3rd pass found 1 HIGH (DOC-001: EXIT row implied a working pre-invocation protection that can't actually fire before a wrong resolution posts) plus MEDIUM DOC-002-007 (loop wording, CLAUDE.md/README propagation, dangling spec pointers, missing /loop EXIT row, wrong cross-reference, smoke test doesn't verify which code-review resolved). Fixed all MEDIUM+ items directly (LOW cosmetic items DOC-009-011 left as-is, auditor itself labeled them optional). Diminishing returns at 4 audit passes — proceeding to QA-RED without a 4th Doc Gate re-run; qa-engineer/qa-auditor will exercise these paths for real.
2026-08-18T02:08:36Z — QA-RED — qa-engineer wrote two headless smoke tests scoped to Task 1 (Input Resolution + Initial Review mode): tests/smoke/test_pr_reviewer_github_initial_review.sh (octocat/Hello-World PR #1 — host detection, code-review delegation without --comment, resolution-identity check via before/after comment count, draft auto-save, no source-branch checkout) and tests/smoke/test_pr_reviewer_gitlab_initial_review.sh (gitlab-org/gitlab-shell MR !1529 — same shape, self-implemented GitLab path), runnable together via tests/smoke/run_pr_reviewer.sh. Followed release-manager's qa-engineer smoke-test pattern (git history only, that suite lives on feature/release-manager). Ran both against this pre-implementation worktree: both FAIL, confirmed for the right reason (no agents/pr-reviewer.md — output lacks pr-reviewer's own mode-detection/host-detection language and no docs/.reviews/ draft was written; a generic fallback handled the request instead). Noted for future runs: nested `claude -p` needs the sandbox disabled in this environment (blocked by the auto-mode classifier otherwise). Handing off to software-engineer for the green phase.
2026-08-18T06:22:22Z — QA-RED (fix-cycle re-entry, TEST FIX REQUEST from software-engineer) — Loaded superpowers:test-driven-development, then read STATE.md/HISTORY.md for full context. Verified software-engineer's finding first-hand rather than taking it on faith: re-read the plan's own Task 4 Step 6 smoke test and found it uses the exact same bare-number prompt shape ("review PR #1 on this repo") the handoff flagged as unreliable — so the handoff's claim that Task 4 Step 6 "already uses full URLs" doesn't hold up against the plan text, though the underlying diagnosis (bare-number prompts dispatch unreliably) is independently well-evidenced by software-engineer's 7-run transcript inspection and matches this session's own testing below.

Rewrote both smoke tests per software-engineer's two suggested fixes, combined: (1) prompt changed to explicit-agent-naming + full-URL ("Use the pr-reviewer agent to review <full PR/MR URL>"), stacking both empirically-reliable forms rather than picking one; (2) primary assertion moved from grepping the outer `claude -p` process's raw/paraphrased text onto the deterministic `docs/.reviews/<host>-<owner-repo>-<number>.md` artifact path itself — exact-path existence, non-empty, and a content-shape check for pr-reviewer's own Draft Phase format (a `## Finding N — ...` heading per finding, or an explicit no/0/zero-findings note). Raw output is still captured/printed for debugging but no longer gates pass/fail, per the CLI-compression confound software-engineer flagged (caveman plugin).

Ran both tests live against the current, UNMODIFIED agents/pr-reviewer.md (never touched this file): github test passed 4/4 consecutive live runs, gitlab test passed 4/4 (after one in-flight fix below), `tests/smoke/run_pr_reviewer.sh` passed 2/2 in a combined run. Caught one test bug of my own during this verification, per TDD's "watch it fail for the right reason" discipline: the gitlab test's first Draft Phase content-shape regex only matched the literal substring "no findings", but a live 0-findings run produced "**0 findings.**" and, on another run, "Zero findings" — neither matched, giving a false FAIL unrelated to pr-reviewer's own correctness. Broadened the regex from `no findings` to `(no|0|zero) findings?` and re-ran to confirm green, including specifically reproducing a 0-findings run to prove the fix. `claude plugin validate . --strict` still passes.

Updated STATE.md (Phase unchanged: QA-RED, mid-fix-cycle; Handoff to: software-engineer) and this HISTORY.md entry per the fix-cycle re-entry protocol. agents/pr-reviewer.md remains untouched and uncommitted — handing back to software-engineer to re-verify green (should reproduce identically, since pr-reviewer's own file is unchanged) and make Task 1 Step 7's commit.
2026-08-18T14:30:00Z — IMPLEMENT (fix-cycle re-entry, re-verification) — Re-verified green per dispatch instructions before making Task 1 Step 7's commit; found a new false-FAIL of the same class qa-engineer had just fixed. Ran the github smoke test 3 live times and the gitlab smoke test 1 live time against the CURRENT, unmodified agents/pr-reviewer.md (file untouched this session). Github run 1 FAILED: the saved docs/.reviews/github-octocat-Hello-World-1.md used the phrasing "### Findings: 0" (zero-indicator token AFTER the word "Findings"), which test_pr_reviewer_github_initial_review.sh line 84's regex `(no|0|zero) findings?` doesn't match (it only matches the token immediately BEFORE "finding(s)"). Github runs 2-3 passed; gitlab run 1 passed (real findings, no zero-findings phrasing at play). Confirmed via re-reading agents/pr-reviewer.md's Draft Phase (Task 1 Step 3) that this is expected, correct model behavior, not an implementation gap: only the CHAT-PRESENTED first line has a mandated literal template; the file saved to docs/.reviews/ is only required to contain "an explicit no-findings note" with no literal wording mandated, so word-order variance is within contract. Raised a second TEST FIX REQUEST to qa-engineer (not force-editing pr-reviewer.md to match an assertion the plan never specifies) with a concrete suggested order-independent regex fix for both test files' line ~84/88. Updated STATE.md (Phase unchanged: QA-RED; Handoff to: qa-engineer). agents/pr-reviewer.md remains untouched and uncommitted — Task 1 Step 7's commit deferred again until tests are reliably green.
2026-08-18T07:18:15Z — QA-RED (fix-cycle re-entry, second TEST FIX REQUEST from software-engineer) — Loaded superpowers:test-driven-development, read STATE.md/HISTORY.md. software-engineer's finding: a second false-FAIL of the same class already fixed once this session, this time from reversed word order ("### Findings: 0" -- zero-indicator token AFTER "finding(s)" instead of before), which the prior fix's regex `(no|0|zero) findings?` still didn't cover since it only anchored the token immediately before the word.

Rewrote the DRAFT_CONTENT shape check in both tests/smoke/test_pr_reviewer_github_initial_review.sh (~line 80-98) and tests/smoke/test_pr_reviewer_gitlab_initial_review.sh (~line 84-102) to be genuinely order-independent rather than patching the single regex again: first checks for a real per-finding heading (`^## Finding [0-9]+`), else checks per-line whether the word "finding" co-occurs with a standalone no/0/zero token in either order (`echo "$DRAFT_CONTENT" | grep -iE 'finding' | grep -qiE '(^|[^a-zA-Z])(no|0|zero)([^a-zA-Z]|$)'`), replacing the old `elif ! echo ... | grep -qiE '...'` one-liner with an explicit SHAPE_OK variable for clarity. Verified offline first (bash -n syntax check plus a standalone script exercising 11 phrasing variants: "0 findings", "no findings", "No findings.", "zero findings", "Zero findings", "### Findings: 0", "Finding count: 0", "**0 findings.**", "There are zero (0) findings to report.", and two real "## Finding N" headings -- all correctly SHAPE_OK=1; plus 3 unrelated-text samples with a bare 0/no token but no "finding" co-occurrence, correctly SHAPE_OK=0) before spending any live runs.

Ran both tests live against the CURRENT, UNMODIFIED agents/pr-reviewer.md (never touched this file, confirmed via git status showing it still untracked/uncommitted): github test passed 4/4 consecutive live runs (all real-findings content this round, no zero-findings phrasing reproduced to re-exercise directly, but the offline unit check above already covers that exact case plus the specific "### Findings: 0" string that caused the original false-FAIL), gitlab test passed 3/3 consecutive live runs. Launched `bash tests/smoke/run_pr_reviewer.sh` as a combined end-to-end confirmation. `claude plugin validate . --strict` still passes.

Updated STATE.md (Phase unchanged: QA-RED, mid-fix-cycle; Handoff to: software-engineer) and this HISTORY.md entry per the fix-cycle re-entry protocol. agents/pr-reviewer.md remains untouched and uncommitted -- handing back to software-engineer to re-verify green (should reproduce identically, since pr-reviewer's own file is unchanged) and make Task 1 Step 7's commit.
- 2026-08-18T07:29:12Z IMPLEMENT: re-verified tests/smoke/run_pr_reviewer.sh green (2/2), committed agents/pr-reviewer.md (1f88a80) and tests/smoke/ (a685c2a). Task 1 complete. Handing off to qa-auditor (Task-1-scoped).
2026-08-18T07:39:04Z — QA-AUDIT — qa-auditor independent re-verification, Task-1-scoped. Rerun tests/smoke/run_pr_reviewer.sh (sandbox disabled, live claude -p calls): 2/2 pass. Verified the code-review name-collision residual risk directly: both tests' before/after comment-count checks against the real public targets (octocat/Hello-World#1, gitlab-shell!1529) passed within their own runs, confirming zero unexpected comments/discussions appeared and the built-in code-review resolved (not the marketplace plugin). Code quality review against plan Task 1 Steps 1-7: all match, no blocking findings — two sub-HIGH observations noted for visibility (bare-number input parsing added beyond spec wording, now untested by the final test suite; Draft Phase auto-save-immediately behavior vs. spec's "once user confirms final" wording, a deliberate TDD-validated headless-invocation adaptation). Conditional security check (mandatory-save + no-checkout, per dispatch instruction) confirmed correctly implemented via the rerun tests' own assertions. .harness/ absent, skipped. claude plugin validate . --strict passes. Clean pass — no HIGH+ findings. Handing off to documentation-auditor (Doc Post-Impl), Task-1-scoped.
2026-08-18T11:03:56Z — DOC-POST-IMPL — documentation-auditor found 1 HIGH (DOC-001: draft save-timing contradiction between HARD REQUIREMENTS and Initial Review Step 3). Fixed across agents/pr-reviewer.md, spec, plan (committed b418470). User chose "Publish now" for Task-1-only partial capability over holding for Tasks 2-4; frontmatter description/WORKFLOW INTENT trimmed to accurately claim only Initial Review mode (committed 8b4fbdf). Re-ran tests/smoke/run_pr_reviewer.sh against trimmed file: 2/2 pass. Handing off to task-orchestrator (Publish Mode).
