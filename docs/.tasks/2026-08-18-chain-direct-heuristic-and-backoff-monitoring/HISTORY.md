# History: chain-direct-heuristic-and-backoff-monitoring

<!-- Append-only. One line per completed phase, in order. Not full detail — that lives in git history / actual test output. -->

- 2026-08-18T05:47:11Z PLAN: task-orchestrator created worktree (.worktrees/feature-chain-direct-heuristic-and-backoff-monitoring) and branch (feature/chain-direct-heuristic-and-backoff-monitoring) from docs/.plans/2026-08-18-chain-direct-heuristic-and-backoff-monitoring.md; Environment Preflight no-op (no .harness/environment.md); qa-engineer + software-engineer feasibility read both ok; no ticket sync configured (no TRACKER.md); handing off to documentation-auditor (Doc Gate) via detached Unattended tmux session.
- 2026-08-18T05:49:00Z HANDOFF NEEDED: detached tmux launch (`tmux new-session -d -s feature/chain-direct-heuristic-and-backoff-monitoring ...`) was denied by the Claude Code auto-mode permission classifier ("Blocked by classifier") — not a tmux/environment failure (tmux 3.7b confirmed present and working). Stopped per task-orchestrator's Unattended rule rather than retrying/working around the denial. Resume options recorded in STATE.md Key info: (1) grant permission and re-trigger to retry the detached launch, or (2) switch this task to Attended and continue synchronously. Worktree/branch/task-folder/feasibility work is already complete and will be reused, not recreated, on resume.
- 2026-08-18T05:49:15Z PLAN: Unattended launch denied by permission classifier (same as release-manager). Switched to Attended, driving the rest of the chain live in this session.
- 2026-08-18T05:56:08Z PLAN: Doc Gate found 3 in-scope HIGH (DOC-001/002 heuristic paragraph contradicted the flow bullets + undefined "Direct-with-worktree" term, DOC-003 backoff mode unreachable given existing Input resolution/Attended-vs-unattended sections) plus 6 in-scope MEDIUM (DOC-004 conflicting fingerprint definitions, DOC-005 dangling pr-reviewer reference, DOC-006 STALLED not durable, DOC-007 HANDOFF NEEDED tasks dropped from monitoring, DOC-008 unusable smoke test, DOC-011 README/CLAUDE.md not updated for new mode). All fixed. 2 pre-existing MEDIUM (DOC-009/010, /cairn-feedback undocumented) noted, not blocking, unrelated. Re-running Doc Gate before resuming to QA-RED.
- 2026-08-18T06:03:20Z PLAN: Doc Gate 2nd pass found 1 in-scope HIGH (DOC-001: rewritten Stale detection dropped the PUBLISH/HANDOFF NEEDED terminal-state carve-out while the prior fix kept HANDOFF NEEDED tasks in the discovery set — combined, a legitimately paused task would get STALLED and its tmux session killed) plus 8 MEDIUM/LOW (flow-bullet consistency, command description/intro stale, marker moved off Harness flags to Status, HISTORY.md line-count definition, dead read path, imprecise wording, fixture format, Invocation carve-out symmetry). All fixed. 2 pre-existing HIGH (/cairn-feedback undocumented) reconfirmed present, unrelated, non-blocking. Given DOC-001's severity (could destroy a live tmux session), re-running Doc Gate a 3rd time before resuming to QA-RED.
- 2026-08-18T06:10:07Z PLAN: Doc Gate 3rd pass — 0 CRITICAL/HIGH, core HANDOFF NEEDED/PUBLISH safety property confirmed airtight. Fixed 5 cheap in-scope MEDIUMs (Global Constraints carve-out, PUBLISH final-message reachability, first-tick baseline, STALLED-marker clearing rule, Stale detection wording). Advancing to QA-RED.
- 2026-08-18T06:22:40Z QA-RED: wrote tests/smoke/test_cairn_run_task_backoff_handoff_safety.sh — headless multi-tick smoke test asserting a Mode: Unattended task at Phase: HANDOFF NEEDED with an unchanging fingerprint never gets STALLED written to STATE.md/HISTORY.md and its tmux session is never killed, across 3 simulated ticks against a two-task fixture. Confirmed RED: fails on the "empty-target falls back to asking which task to check" signal (backoff loop mode not implemented) in all 3 ticks, while the STALLED/tmux-kill assertions themselves already pass clean, confirming the test is wired correctly. Task 1 (read-back self-review) and Task 3 (version bump) have no scripted test per the plan's own text. Handing off to software-engineer.
- 2026-08-18T07:05:00Z IMPLEMENT: implemented Task 2's commands/cairn-run-task.md/README.md text (already matched plan verbatim from a prior interrupted pass). Investigated a reported tick-1 tmux-session-kill failure: confirmed via full-file grep no reachable `tmux kill-session` outside the STALLED branch, confirmed the working-tree text is byte-identical to the plan's prescribed Task 2 text, and reran the smoke test/isolated ticks 11 additional times (3 with stream-json tool-call tracing) with zero reproductions of the kill. Added a defense-in-depth "this mode is tmux-read-only except the STALLED branch" guardrail sentence regardless. Found and reproduced (2/5 post-guardrail runs) a genuine test bug instead: tests/smoke/test_cairn_run_task_backoff_handoff_safety.sh:152's disambiguation regex false-positives on the literal phrase "no target given" appearing in correct narration. Raising TEST FIX REQUEST to qa-engineer rather than force-editing the test.
- 2026-08-18T07:22:00Z QA-RED (fix cycle): fixed tests/smoke/test_cairn_run_task_backoff_handoff_safety.sh:152 — dropped the false-positiving `no target given` regex alternative (redundant with `which (one|task)|please (specify|provide)`, which already cover the real signal). Verified 5/5 consecutive full runs against the current implementation with zero false positives, plus a control run against the stashed pre-implementation state confirming the tightened regex still correctly catches genuine disambiguation-fallback output. Implementation files untouched. Handing back to software-engineer for Task 2 Step 8 commit + Task 3.
- 2026-08-18T06:56:53Z IMPLEMENT: software-engineer committed Task 2 (8ca0a5a) and Task 3 version bump (97c3ee9, 0.17.0->0.18.0 read from main). Dispatch ended without finishing bookkeeping - verified independently: claude plugin validate --strict passes, tests/test_usage_dashboard.py 29/29 green. Handing off to qa-auditor.
- 2026-08-18T07:35:00Z IMPLEMENT: re-verified the smoke test clean (1/1), confirmed the Task 2 Step 8 commit and Task 3 version bump were already made (8ca0a5a, 97c3ee9), then ran Task 3 Step 2's full final validation to completion: claude plugin validate . --strict passes clean; pytest tests/ -v -s — 30 passed (test_usage_dashboard.py 29/29, test_intent_routing.py aggregate 16/17, above MIN_PASS). All three plan tasks fully implemented and committed. Handing off to qa-auditor.
- 2026-08-18T07:08:46Z QA-AUDIT: qa-auditor's dispatch ended mid-verification (waiting on its own background batch). Completed independently instead: 3 more clean runs of the safety test (total ~19 clean runs across 3 independent verification rounds, zero reproductions of the tmux-kill issue), plus a direct code read confirming exactly one tmux kill-session call in commands/cairn-run-task.md, correctly guarded by the Phase-not-PUBLISH-or-HANDOFF-NEEDED condition, with an explicit "this mode is read-only w.r.t. tmux" guardrail outside it. Confirmed CLAUDE.md's Chain/Direct flow bullets both acknowledge the heuristic can redirect between them. Clean pass. Handing off to documentation-auditor (Doc Post-Impl).
- 2026-08-18T07:14:56Z DOC-POST-IMPL: documentation-auditor found 1 in-scope HIGH (DOC-001: stale duplicate Stale-detection definition in agents/task-orchestrator.md, pointed at wrong write-target) + 2 in-scope MEDIUM (DOC-002 guardrail self-contradiction, DOC-003 tests/smoke/ undocumented). All fixed. Safety carve-outs verified intact in shipped files across all locations. 2 pre-existing MEDIUM (/cairn-feedback undocumented) reconfirmed present, unrelated, non-blocking. Advancing to Publish.
- 2026-08-18T07:16:58Z QA-AUDIT (independent, this dispatch): a second, concurrently-running process also wrote QA-AUDIT (07:08:46Z) and DOC-POST-IMPL (07:14:56Z) entries above while this dispatch's own verification was still in flight — noted for the record, not reverted (working tree already carries commits b009eee/84d8c26 building on that progression, and DOC-001 there is the same task-orchestrator.md drift this dispatch independently found via `grep -rn kill-session`/`STALLED`, corroborating rather than conflicting). This dispatch's own independent evidence, gathered separately: (1) full static read of commands/cairn-run-task.md end to end — exactly one `tmux kill-session` call in the file, gated by item 4's "Phase not PUBLISH/HANDOFF NEEDED" condition and restated unconditionally in Stale detection's own paragraph; Resume/Monitor/Input-resolution/Attended-vs-unattended sections carry no other STALLED/kill-session path. (2) 8-run backgrounded batch of tests/smoke/test_cairn_run_task_backoff_handoff_safety.sh: runs 1-7 clean, run 8 tick 2 saw both fixture tmux sessions killed with the command's own narration explicitly claiming no tmux action taken — investigated rather than waved off; no `claude -p`/tmux-server activity of this dispatch's own remained by the time it was checked, and the failure window (07:07:38-07:09Z) overlaps the other process's own test activity implied by its 07:08:46Z entry above, both scripts using the same hardcoded (non-namespaced) tmux session names `feature/fake-handoff-task`/`feature/fake-other-task` — consistent with two concurrent test invocations colliding on shared session names (the fixture script's own unconditional kill-session-then-recreate at setup, tests/smoke/test_cairn_run_task_backoff_handoff_safety.sh:111-112) rather than a defect in the shipped command. (3) 3 additional isolated single-shot reruns immediately after, with zero other claude/tmux activity confirmed present — all 3 clean, no STALLED, no session kill. Verdict: safety property holds; the one anomalous tick has an evidenced innocent explanation (test-fixture session-name collision under concurrent execution, not a code defect), distinct in kind from being dismissed on faith. Flagging test-quality note for qa-engineer (LOW, non-blocking): the smoke test's hardcoded tmux session names aren't safe under concurrent invocations of the same script; scoping them to `$SCRATCH_DIR`/a PID/random suffix would remove this false-failure mode. Code-quality pass (CLAUDE.md/commands/cairn-run-task.md/README.md text vs. plan): verbatim match on all Task 1/Task 2 prescribed text; Task 1's flow-bullet amendments (Direct/Chain flow bullets acknowledging the heuristic can redirect between them) confirmed landed. No new findings beyond what documentation-auditor already caught and fixed above. Concurring clean-pass verdict on this dispatch's own evidence — no further phase/handoff change made here since the chain already correctly advanced past this point in the interim.
- 2026-08-18T14:20:00Z PUBLISH: task-orchestrator made the consolidated commit (84d8c26, adding the task folder itself as permanent history), detected GitHub as origin's remote host, pushed the branch, and opened https://github.com/jisundr/cairn/pull/4. Wrote UAT.md. Usage report unavailable (predates timestamp tracking) — omitted per documented fallback, never blocking. No ticket sync configured — Status Sync steps no-op, plan file retained. .harness/ absent entirely — consolidated drift-flag question skipped (Harness flags empty, no new/blocking Doc Post-Impl findings). Published as-is. Terminal.
